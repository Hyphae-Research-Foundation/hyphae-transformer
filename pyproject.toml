[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hyphae-transformer"
version = "0.2.0"
description = "A governed transformer research framework, frozen-backbone control trainer, and reproducible architecture lab"
readme = "README.md"
requires-python = ">=3.12,<3.14"
license = { file = "LICENSE" }
authors = [
  { name = "Celiums Solutions LLC" },
  { name = "Mario Gutierrez" },
]
keywords = ["rezero", "transformer", "deep-learning", "research", "pytorch"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
  "numpy>=2.0,<3",
  "torch>=2.6,<3",
]

[project.optional-dependencies]
dev = [
  "mypy>=1.15",
  "pytest>=8.3",
  "pytest-cov>=6",
  "ruff>=0.11",
]

[project.scripts]
hyphae-transformer = "celiums_rezero.cli:main"
hyphae-knowledge = "celiums_rezero.knowledge.cli:main"
celiums-rezero = "celiums_rezero.cli:main"
celiums-knowledge = "celiums_rezero.knowledge.cli:main"
hyphae-governed-runtime = "celiums_rezero.governed.runtime:main"
hyphae-quoted-runtime-canary = "celiums_rezero.knowledge.canary_runtime:main"

[project.urls]
Repository = "https://github.com/Hyphae-Research-Foundation/hyphae-transformer"
Paper = "https://proceedings.mlr.press/v161/bachlechner21a.html"

[tool.hatch.build.targets.wheel]
packages = ["src/celiums_rezero"]

[tool.hatch.build.targets.sdist]
exclude = ["/.tmp-cert", "/artifacts", "/runs", "/checkpoints"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.ruff.lint.isort]
known-first-party = ["celiums_rezero"]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src"]

[tool.pytest.ini_options]
addopts = "-ra --strict-markers --strict-config"
testpaths = ["tests"]
